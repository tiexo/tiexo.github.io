
# .vscode/preview.yml
autoOpen: true # 打开工作空间时是否自动开启所有应用的预览
apps:
  - port: 4000 # 应用的端口
    run: hexo clean&hexo g&hexo s # 应用的启动命令
    root: / # 应用的启动目录
    name: 在线预览 # 应用名称
    description: hexo clean&hexo g&hexo s # 应用描述
    autoOpen: false # 打开工作空间时是否自动开启预览（优先级高于根级 autoOpen）
